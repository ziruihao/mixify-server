// import mongoose, { Schema } from 'mongoose';

// const UserSchema = new Schema({
//   name: String,
//   token: String,
//   topTracks: [{type: Schema.Types.ObjectId, ref: 'Track'}],
//   genres: [String],
// }, {
//   toJSON: {
//     virtuals: true,
//   }
// });

// const UserSchema = mongoose.model('User', UserSchema);

// export default UserSchema;